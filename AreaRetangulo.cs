using System;
class AreaRetangulo
{
    static void Main (String [] args)
    {
        int Base, altura, area;
        Base = 14;
        altura = 8;
        area = Base * altura;
        Console.WriteLine("A area do retangulo e: " + area);
    }

}